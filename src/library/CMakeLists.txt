cmake_minimum_required(VERSION 3.28)

add_library(mtlib OBJECT ${LIBRARY_SOURCE_FILES})
add_dependencies(mtlib elr_files)
if (BUILD_SEPARATED)
    target_sources(mtlib PRIVATE variables.cpp)
endif ()
target_compile_definitions(mtlib PUBLIC ${MTLIB_PUBLIC_DEFS} PRIVATE ${MTLIB_PRIVATE_DEFS})
target_link_libraries(mtlib PRIVATE RapidJSON rapidjson)

add_library(mtnclib OBJECT ${LIBRARY_SOURCE_FILES})
add_dependencies(mtnclib elr_files)
if (BUILD_SEPARATED)
    target_sources(mtnclib PRIVATE variables.cpp)
endif ()
target_compile_definitions(mtnclib PUBLIC ${MTNCLIB_PUBLIC_DEFS} PRIVATE ${MTLIB_PRIVATE_DEFS})
target_link_libraries(mtnclib PRIVATE RapidJSON rapidjson)

add_library(stlib OBJECT ${LIBRARY_SOURCE_FILES})
add_dependencies(stlib elr_files)
if (BUILD_SEPARATED)
    target_sources(stlib PRIVATE variables.cpp)
endif ()
target_compile_definitions(stlib PUBLIC ${STLIB_PUBLIC_DEFS} PRIVATE ${STLIB_PRIVATE_DEFS})
target_link_libraries(stlib PRIVATE RapidJSON rapidjson)

add_library(stnclib OBJECT ${LIBRARY_SOURCE_FILES})
add_dependencies(stnclib elr_files)
if (BUILD_SEPARATED)
    target_sources(stnclib PRIVATE variables.cpp)
endif ()
target_compile_definitions(stnclib PUBLIC ${STNCLIB_PUBLIC_DEFS} PRIVATE ${STLIB_PRIVATE_DEFS})
target_link_libraries(stnclib PRIVATE RapidJSON rapidjson)
